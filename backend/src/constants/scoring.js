// backend/src/constants/scoring.js

// A session can't be scored until the user has sent at least this
// many messages. Keep in sync with MIN_USER_MESSAGES_TO_SCORE in
// frontend/src/pages/Session.jsx (used for the disabled-button hint).
export const MIN_USER_MESSAGES_TO_SCORE = 2
